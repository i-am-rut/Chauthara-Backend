const nameConfig = {
    minLength: 1,
    minLengthMessage: "Name must contain atleast 1 character.",
    maxLength: 50,
    maxLengthMessage: "Name can not exceed 50 characters",
}

export default nameConfig;