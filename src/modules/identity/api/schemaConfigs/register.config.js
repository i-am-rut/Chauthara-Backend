const registerConfig = {
    ageConfiremd: {
        message: "Age must be confirmed."
    },
    termsAccepted: {
        message: "Terms must be accepted."
    },
    confirmPasswordRequired: "Confirm Password is required."
}

export default registerConfig;